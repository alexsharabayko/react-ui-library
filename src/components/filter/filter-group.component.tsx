import React, { PropsWithChildren, ReactElement } from 'react';
import { IFilterGroup } from './filter.domain';

/**
 * FilterGroup Component Description
 */
export const FilterGroup = ({}: PropsWithChildren<IFilterGroup>): ReactElement => {
  return null;
};
