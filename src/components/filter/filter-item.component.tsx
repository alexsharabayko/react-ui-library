import React, { PropsWithChildren, ReactElement } from 'react';
import { IFilterItem } from './filter.domain';

/**
* FilterItem Component Description
*/
export const FilterItem = ({}: PropsWithChildren<IFilterItem>): ReactElement => {
  return null;
};
